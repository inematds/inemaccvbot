import type { SkillDef } from './skills.js';

export function skillsText(defs: SkillDef[]): string {
  return ['🎬 skills registradas:', ...defs.map((d) => `• ${d.command} — ${d.description}\n  ex.: ${d.example}`)].join('\n');
}

export function helpText(defs: SkillDef[], dests: string[]): string {
  return [
    '🤖 inemaccvbot — DUAS filas de jobs (mesmo daemon mkivideos, bancos/dashboards separados)',
    '  🎬 fila de vídeo (mkivideos.service) — explicativo, curso, demo — render, ~15min/job',
    '  📝 fila de texto (mkitexto.service) — transcrever, dublar — via inemavox, minutos/job',
    'Separadas pra um pedido de texto não esperar atrás de um render; uma fila fora do ar nunca',
    'bloqueia a outra.',
    '',
    'Mande instruções em texto — uma instrução por linha = um job:',
    '  <skill>: <assunto ou link> [| 9:16] [| pesquisa] [| narracao] [| transcrever] [| livesN] [| modulo X] [| curso X]',
    '',
    'Exemplos:',
    ...defs.map((d) => `  ${d.example}`),
    '  explicativo: Computação quântica | pesquisa | lives2',
    '  explicativo: O que é RAG | narracao',
    '',
    'Campos (qualquer ordem):',
    '  9:16 ou vertical — Shorts/Reels (default 16:9)',
    '  pesquisa — pesquisa o assunto na web antes de escrever o roteiro',
    '  narracao ou texto — manda a narração completa em texto além do vídeo (mensagem ou arquivo)',
    '  transcrever ou transcricao — CAMPO de job de VÍDEO: baixa e transcreve o áudio de origem',
    '    (inemavox, Whisper local) e usa como base do roteiro; falha derruba o job (vê em /status)',
    `  livesN — move o resultado pra yt-pub-livesN/imports/videos (válidos: ${dests.join(', ') || 'nenhum'})`,
    '',
    'Atenção, não confundir "transcrever": CAMPO (job de vídeo, ex.: "explicativo: <link> |',
    'transcrever") GERA VÍDEO usando o áudio como base; SKILL própria (fila de texto, "transcrever:',
    '<link>") NÃO gera vídeo, devolve só o TEXTO.',
    '',
    'Texto livre também funciona (o bot interpreta), mas só com as skills registradas.',
    '',
    'Arquivo anexado (ex.: .md) com legenda vira fonte do vídeo (ex.: "explicativo: resumo desse',
    'documento"). Sem legenda, o bot só pergunta, sem enfileirar.',
    '',
    'reel — vídeo 9:16 a partir de um avatar HeyGen (skill reel-edita-inema). Input é o MP4:',
    '  • /reel /home/.../avatar.mp4 quero com texto e imagem ilustrativa — comando dedicado',
    '    (recomendado, não se confunde com texto livre); aceita descrição solta ou "| campos".',
    '  • CAMINHO — "reel: /home/.../avatar.mp4 | lives3" — avatares passam de 20 MB, acima do',
    '    limite de download do bot, então caminho é a via confiável.',
    '  • ANEXO — só <20 MB — .mp4 com legenda "/reel" ou "reel" (ou "reel | lives3 | mover").',
    'Campos do reel: livesN COPIA o resultado (original fica em ~/projetos/output/<slug>/); "|',
    'mover" MOVE em vez de copiar; "| visuais" usa Modo 3 (visuais) em vez do explicador (Modo 2);',
    'dá pra pedir "imagem/ilustrativa" em texto solto que o bot ativa visuais sozinho.',
    '  • PASTA — "/reel /caminho/da/pasta/ [descrição]" enfileira UM reel por vídeo (.mp4/.mov/.m4v/',
    '    .webm, só topo, sem recursão), mesma descrição/flags pra todos; resposta resume quantos e o',
    '    intervalo de ids (ex.: "12 reels na fila: V#90…V#101"); pasta vazia não enfileira nada.',
    '',
    'reelinematds — reel pessoal estilo INEMATDS a partir de bruto vertical (skill reel-edita-inematds):',
    '  /reelinematds /home/.../bruto.mp4 [descrição], ou "reelinematds: <caminho> | campos", ou anexo',
    '  <20 MB com legenda igual. Mesmos campos do reel; sem modos, a skill decide o tratamento sozinha.',
    '',
    'Pergunta em linguagem natural sobre o andamento ou o que o bot sabe fazer também funciona,',
    'sem criar job — ex.: "terminou?", "quanto falta pro V12?", "você moveu pro lives3?".',
    '',
    'IDs de job vêm PREFIXADOS — V#12 é da fila de vídeo, T#7 é da fila de texto (por-fila: um V#7 e',
    'um T#7 podem coexistir, são jobs diferentes). Prefixo (com/sem "#", maiúsc./minúsc.) vale em',
    '/status, /cancelar e /enviar. Sem prefixo, só funciona se o número for único no chat.',
    '',
    'Comandos:',
    '  /fila — running + queued com posição, das DUAS filas',
    '  /status [id] — detalhe do job (V#/T#); sem id: visão geral + stats das DUAS filas',
    '  /cancelar <id> — cancela job na fila (V#/T#)',
    '  /enviar <id> — recebe o arquivo aqui (≤50 MB; acima, só o caminho)',
    '  /reel <caminho> [descrição] — atalho pra skill reel (ver acima); sem caminho, orienta',
    '  /skills — o que o bot sabe fazer',
    '  /help — esta ajuda',
  ].join('\n');
}
